<Window x:Class="Check_IT.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Check_IT.ViewModels"
        mc:Ignorable="d"
        Title="Register" Height="320" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Style="{StaticResource AppWindow}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <!-- Name -->
            <RowDefinition Height="Auto"/> <!-- Email -->
            <RowDefinition Height="Auto"/> <!-- Password -->
            <RowDefinition Height="Auto"/> <!-- Spacer -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Visible header for debugging -->
        <TextBlock Text="Register new user" FontSize="18" FontWeight="Bold" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,0,0,8" />

        <TextBlock Text="Name:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,8,6"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,6,0,6" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" />

        <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,8,6"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,6,0,6" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}"/>

        <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,8,6"/>
        <PasswordBox Grid.Row="3" Grid.Column="1" Margin="0,6,0,6" x:Name="PasswordBox" Style="{StaticResource InputPasswordBox}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="RegisterButton" Width="100" Margin="0,0,8,0" Click="RegisterButton_Click" Style="{StaticResource PrimaryButton}">Register</Button>
            <Button Width="100" Click="CancelButton_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>