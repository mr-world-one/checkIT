<UserControl x:Class="Check_IT.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Check_IT.ViewModels"
             xmlns:conv="clr-namespace:Check_IT.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <conv:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter" />
    </UserControl.Resources>

    <Border Padding="20" Background="White" CornerRadius="8" Width="480" HorizontalAlignment="Center" VerticalAlignment="Top">
        <StackPanel>
            <TextBlock Text="User Registration" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10" />

            <TextBlock Text="{Binding Error}" Foreground="#dc3545" Background="#f8d7da" Padding="10" TextAlignment="Center" />

            <StackPanel Margin="0,8,0,8">
                <TextBlock Text="Name" FontWeight="SemiBold" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="0,8,0,8">
                <TextBlock Text="Email" FontWeight="SemiBold" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="0,8,0,8">
                <TextBlock Text="Password" FontWeight="SemiBold" />
                <PasswordBox x:Name="PasswordBox" />
            </StackPanel>

            <Button Content="Register" Width="180" HorizontalAlignment="Center" Margin="0,10,0,0" Command="{Binding RegisterCommand}" Click="RegisterButton_Click" />
        </StackPanel>
    </Border>
</UserControl>