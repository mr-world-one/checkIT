<UserControl x:Class="Check_IT.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Check_IT.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.DataContext>
        <vm:HomeViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#3BD24A" Offset="0" />
            <GradientStop Color="#10B981" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="32"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="20" ShadowDepth="4" Direction="270"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="30" ShadowDepth="8" Direction="270"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3bd24a"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2fa33f"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFF1F1"/>
            <Setter Property="Foreground" Value="#8B1E1E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE6E6"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFDBDB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F9FAFB">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- WebView2 для анімації заголовка -->
        <wv2:WebView2 x:Name="HeaderWebView" 
                      Grid.Row="0" 
                      Height="550"
                      Margin="0"/>

        <!-- Основний контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40" MaxWidth="1200">
                <!-- Картки функцій -->
                <Grid x:Name="cardsPanel">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <Border Background="#EEF2FF"
                                   Width="64"
                                   Height="64"
                                   CornerRadius="12"
                                   Margin="0,0,0,24">
                                <TextBlock Text="📊"
                                          FontSize="32"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock Text="Аналіз Prozorro"
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="#111827"
                                      Margin="0,0,0,12"/>

                            <TextBlock Text="Перевірте цілі тендерів та порівняйте їх із ринковими цінами для виявлення аномалій"
                                      FontSize="14"
                                      Foreground="#6B7280"
                                      TextWrapping="Wrap"
                                      LineHeight="22"
                                      Margin="0,0,0,32"/>

                            <Button Content="Увійти та дослідити"
                                   Style="{StaticResource ModernButton}"
                                   HorizontalAlignment="Stretch"
                                   Click="OpenProzorro_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <Border Background="#ECFDF5"
                                   Width="64"
                                   Height="64"
                                   CornerRadius="12"
                                   Margin="0,0,0,24">
                                <TextBlock Text="📈"
                                          FontSize="32"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock Text="Аналіз Excel"
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="#111827"
                                      Margin="0,0,0,12"/>

                            <TextBlock Text="Завантажте тендери в Excel і отримайте детальне порівняння цін з ринковими показниками"
                                      FontSize="14"
                                      Foreground="#6B7280"
                                      TextWrapping="Wrap"
                                      LineHeight="22"
                                      Margin="0,0,0,32"/>

                            <Button Content="Увійти та перевірити"
                                   Style="{StaticResource ModernButton}"
                                   HorizontalAlignment="Stretch"
                                   Click="OpenExcel_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="4" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <Border Background="#FEF3C7"
                                   Width="64"
                                   Height="64"
                                   CornerRadius="12"
                                   Margin="0,0,0,24">
                                <TextBlock Text="👤"
                                          FontSize="32"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock Text="Створіть акаунт"
                                      FontSize="24"
                                      FontWeight="Bold"
                                      Foreground="#111827"
                                      Margin="0,0,0,12"/>

                            <TextBlock Text="Отримайте повний доступ до усіх функцій порівняння після швидкої реєстрації"
                                      FontSize="14"
                                      Foreground="#6B7280"
                                      TextWrapping="Wrap"
                                      LineHeight="22"
                                      Margin="0,0,0,32"/>

                            <Button Content="Зареєструватися"
                                   Style="{StaticResource ModernButton}"
                                   HorizontalAlignment="Stretch"
                                   Click="OpenRegisterBtn_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>